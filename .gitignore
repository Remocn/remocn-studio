# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
*.local

# Next.js
.next
out
next-env.d.ts
.turbo

# tsc --noEmit with "incremental": true
*.tsbuildinfo

# `bun run sidecar:build` output, bundled into the app as a Tauri resource
sidecar-dist

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.omc

# Agent worktrees, created by Claude Code's worktree isolation
.claude/worktrees/

# Vendored reference checkouts (read-only material for agents, see CLAUDE.md).
# Third-party source — cloned locally on purpose, never committed.
repos/
